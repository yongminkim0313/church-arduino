# TalentNfcReader — ESP32-S3 NFC 키링 리더 (펀펀포인트)

NTAG-213 키링을 PN532(I2C)로 읽어 2.8" ILI9341 TFT 에 포인트를 보여준다.
입력은 디스플레이에 붙은 XPT2046 터치 패널 하나뿐이다.

화면 미리보기: [`../TalentNfcReader.html`](../TalentNfcReader.html) — 브라우저에서 열면
태깅·내역·오류 화면을 실제 렌더링 그대로 만져 볼 수 있다.

## 처리 순서

"누구에게" 와 "무엇을 얼마나" 를 **NFC 두 장**으로 나눠 받는다. 어린이 키링이 "누구",
지급/사용 카드가 "무엇을 얼마나" 다.

```
대기 ── 키링 ──▶ 카드 대기 ── 카드 ──▶ 완료 ──(doneMs)──▶ 대기
              이름·잔액           그림·얼마
              ·쓸 수 있는 카드 목록  ·남은 포인트
                   │
                  취소 ──▶ 대기
```

한 번에 처리하지 않는 이유: 잘못 댄 키링을 **되돌릴 자리**가 있어야 한다. 예전에는
태깅 한 번으로 곧바로 잔액이 움직여서, 틀리면 관리자가 화면에서 손봐야 했다.

되돌릴 자리는 **카드 대기 화면의 '취소'** 다. 확인 단추는 따로 두지 않는다 —
확인할 것(누구에게)은 이름·잔액이 이 화면에 떠 있어 카드를 대기 전에 눈으로 보고,
무엇을 얼마나는 카드에 적혀 있어 고를 여지가 없다. 줄을 세워 놓고 쓰는 기기라
누르는 횟수가 하나 줄면 체감이 크다.

**사용에서 잔액을 넘으면** 카드를 댄 자리에서 막고 "포인트가 모자랍니다" 를 띄운다.
카드 단계에 그대로 머물러 다른 카드를 대면 된다.

| 동작 | 결과 |
|------|------|
| 키링 태깅 | 이름 · 남은 포인트 · 등록된 카드 목록을 띄우고 **카드**를 기다린다. **출석모드**면 이때 곧바로 지급된다 |
| 취소 (카드 대기 화면) | 진행 중이던 것을 접고 대기로 돌아간다 — 잘못 댄 키링을 무르는 자리 |
| 카드 태깅 | **그 카드가 정한 방향으로** 곧바로 처리하고 완료 그림을 띄운다 (`doneMs` 뒤 저절로 대기로) |
| **헤더 띠 터치** | 전체 내역으로. 다시 누르면 대기 화면으로 돌아온다. 여기서 키링을 대면 잔액을 건드리지 않고 조회만 한다. 두 번 터치와 가르느라 0.45초 뒤에 열린다 |
| 헤더 띠 터치 (끊김일 때) | **블루투스 와이파이 설정** 화면 (아래 참고) |
| **헤더 띠 두 번 터치** | **학생 사진 모두 받기** — 명단을 새로 받고, 사진이 있는 아이마다 없거나 덜 받은 사진을 지금 받는다(진행 막대·이름 표시). 받는 동안 화면을 누르면 멈춘다. 끝나면 새로 받음·이미 있음·실패를 4초 띄운다. 평소에는 대기 중 조용할 때 한 장씩 받으므로, 사진을 한꺼번에 올렸거나 리더를 새로 들였을 때 쓴다 |
| 내역 위·아래 띠 터치 | 위 = 이전 페이지(더 최근), 아래 = 다음 페이지(더 예전) |
| 60초 무입력 | 진행 중이던 것을 스스로 접는다 — 앞사람 이름에 다음 사람이 처리되는 사고 방지 |
| 무입력 | `sleepEnabled` 가 켜진 기기만 딥슬립 (기본 꺼짐) |

### 켤 때 뜨는 화면

전원을 넣고 서버에 붙기까지 몇 초 동안 뜬다. 화면 전체를 덮으므로 **테두리·헤더도
그리지 않는다** — 아직 아무것도 할 수 없는 상태라 단추가 있으면 눌러도 되는 줄 안다.

이 그림만 예외적으로 **파일 이름을 NVS 에 들고 있는다.** 서버에 붙기 *전에* 그려야
하는데, 그때는 설정을 아직 못 받았기 때문이다. 그래서 **그림을 바꾼 뒤 한 번은 예전
그림이 뜬다**(그 부팅에서 새로 받고, 다음 부팅부터 새것). 처음 켤 때는 아직 아무것도
없어서 바탕색에 "연결 중" 글자 한 줄로 대신한다.

### 헤더 띠

로고·제목·연결 표시가 들어가는 맨 위 띠다. **띠 전체가 내역 단추**다 — 작은 아이콘
하나보다 훨씬 누르기 쉽고, 관리자가 올린 띠 그림에 자리를 뺏기지도 않는다. 다시 누르면
대기 화면으로 돌아온다(들어가는 문과 나오는 문이 같아야 헤맬 일이 없다).

관리자 화면에서 **연결됨·끊김 두 장**을 올리면 그 그림이 띠 전체를 대신한다. 한 장만
올리면 쓰이지 않는다 — 연결이 끊겼을 때 화면이 비어 무슨 일인지 알 수 없기 때문이다.

**끊겨 있을 때 이 띠를 누르면 블루투스 와이파이 설정으로 간다.** 여기가 '끊김' 이라고
적혀 있는 자리이고, 끊긴 채로는 내역도 받아 오지 못한다.

### 지급/사용 카드

`GET /api/talent/cards` 로 부팅할 때 받아 둔다. 카드 한 장은 `{ uid, side, name, amount, category }`
이고, 관리자 화면(**지급 · 사용 카드**)에서 등록한다. 여러 장 등록할 수 있다.

**금액은 리더가 정하지 않는다.** 처리할 때 `POST /earn|/spend` 에 `card` UID 만 보내고,
서버가 그 카드에서 금액·이름·분류를 찾아 쓴다. 리더는 현장에 놓인 기기라 요청을 흉내
내기 쉬워서, 오르내리는 양의 근거를 서버에만 뒀다.

**지급이냐 사용이냐는 카드가 정한다.** 화면에서 고르지 않는다 — 지급 카드를 대면 지급,
사용 카드를 대면 사용이다. 그래서 등록된 카드는 종류를 가리지 않고 모두 카드 대기
화면의 목록에 뜨고, 어느 쪽인지는 금액의 색이 말한다(초록 = 지급, 빨강 = 사용).

잔액으로 감당이 안 되는 사용 카드는 **흐리게** 둔다. 대 보고 나서 "포인트가 모자랍니다"
를 보는 것보다 대기 전에 눈으로 아는 편이 낫다(대도 처리하지 않고 그 화면에 머문다).

### 처음 보는 카드

예전에는 모르는 UID 에 곧바로 잔액을 만들었다. 그래서 휴대폰이나 교통카드가 스쳐도
"이름 없는 사람" 이 생겼다. 이제는 **아무것도 만들지 않고** `POST /api/talent/seen` 으로
알리기만 한다. 관리자 화면의 **미등록카드** 팝업에 **새로고침 없이 곧바로** 뜨고
(서버가 RTDB `/talentSeen` 을 구독해 관리자 소켓으로 `talent:seen` 을 보낸다 —
`yvServer/talent/talentFeed.js`), 방금 댄 카드는 몇 초 동안 '방금 태깅' 으로 물든다.
거기서 셋 중 하나로 정한다:

- **신규 등록** — 새 키링을 받은 어린이
- **기존 사용자 매칭** — 키링을 잃어버려 다시 받은 경우(이름·잔액을 옮긴다)
- **포인트 카드** — 어린이가 아니라 지급/사용 카드

### 출석모드

`attendanceMode` 를 켜면 카드 단계를 건너뛴다. **키링 한 번**으로 바로
지급된다. 주일 아침처럼 줄이 길고 사유가 하나로 정해진 자리를 위한 것이다.
무엇으로 지급할지는 `attendanceCard`(카드 UID)로 정하고, 비워 두면 그 기기에서 쓸 수
있는 첫 카드를 쓴다.

### 지급·사용 탭을 없앤 이유

예전에는 헤더 아래에 **지급·사용 그림 버튼 두 칸(78px)** 이 있었고, 그 탭이 곧 모드였다.
그런데 대는 카드에 이미 방향이 적혀 있어, 탭이 하는 일은 "카드와 맞는 쪽을 미리 골라
두는 것" 뿐이었다. 어긋나면 *"사용 카드입니다"* 로 되돌려 보냈는데 — 아이 입장에서는
맞는 카드를 댔는데 안 되는 화면이다.

탭을 걷어내니 고를 것이 없어지고, 화면은 **78px** 을 되찾았다. 카드 목록이 세 줄 더
들어가고, 배경 그림도 그만큼 커진다.

따라오는 변화:

| | 예전 | 지금 |
|---|---|---|
| 방향을 정하는 것 | 화면의 탭 | **대는 카드** |
| 카드 대기 목록 | 그 탭에 맞는 카드만 | **등록된 카드 전부** (색으로 방향 구분) |
| 안 맞는 카드 | "지급/사용 카드입니다" 로 거절 | 그대로 처리 |
| `modeLock` · `defaultMode` | 탭을 잠그거나 기본 탭을 정함 | **읽지 않는다** (관리자 화면의 값은 무시) |
| 탭 버튼 그림(`tab`) | 서버에서 네 장을 받아 그림 | **쓰지 않는다** (받지도 않는다) |
| 내용 영역 | 192px | **270px** |

### 테두리가 방향을 말한다

`+` / `-` 부호는 쓰지 않는다. 대신 **화면 테두리 4px** 이 방금 무슨 일이 있었는지 알린다:

| 테두리 | 언제 |
|--------|------|
| 회색 | 대기 · 카드 기다림 · 내역 — 아직 방향이 정해지지 않았다 |
| 초록 | 완료 화면 · 지급 |
| 빨강 | 완료 화면 · 사용 |

고르기 전에는 방향이 없으므로 테두리도 회색이다 — 아직 아무 방향도 아닌데 초록이나
빨강을 띄우면 그 색이 거짓말이 된다. 획 하나(`+`/`-`)는 멀리서 안 보이지만 테두리
색은 보인다.

### 서버 그림 규격

탭 줄이 사라진 만큼 **내용 영역이 192 → 270px** 로 늘었다. 기기는 배경의 폭·높이가
정확히 같을 때만 쓰므로, 서버의 그림 규격(`talentArt.js`)도 함께 맞춰야 한다 —
어긋나면 조용히 바탕색으로 떨어진다. **배경은 다시 올려야 한다.**

| 그림 | 규격 |
|---|---|
| 배경(`bg`) | 232×**270** (예전 232×192 — **다시 올려야 한다**) · 한 장만 쓴다(관리자 화면의 '지급 배경') |
| 카드(`card`) | 폭 108 · 높이 ≤ **200** |
| 완료(`done`) | 폭 200 · 높이 ≤ **200** |
| 헤더 띠(`header`) | **232×42** · `on`(연결됨)·`off`(끊김) 두 장 |
| 켤 때 뜨는 화면(`splash`) | **240×320** · 화면 전체 한 장 |

### 바탕색

| 화면 | 내용 영역 바탕 | 이유 |
|----|----------------|------|
| 평소 · 내역 | **설정색** (`bgColor`, 기본 `#EBAC42`) | 관리자 화면에서 고른다. 헤더 띠와 같은 색이라 위에서 아래까지 한 장으로 이어진다 |
| 배경 사진을 올린 기기 | 사진 | 내역에는 깔지 않는다(목록을 정확히 읽어야 하는 자리다). 사진 위에는 늘 흰 글자다 |

목록의 줄무늬와 페이지 띠는 **바탕색에서 한 단계 눌러** 만든다(`bgShade`). 밝은 바탕이면
어둡게, 어두운 바탕이면 밝게 — 어떤 색을 넣어도 뒤집히지 않는다.

내역도 같은 자리를 쓴다. 나올 때는 헤더 띠를 다시 누른다 — 들어가는 문과 나오는
문이 같아야 헤맬 일이 없다.

글자색은 자리마다 정하지 않고 **역할**로 부른다 — `inkMain()` / `inkSub()` /
`inkMuted()` / `inkDir(up)`. 같은 자리라도 바탕이 밝으냐 어두우냐에 따라 읽히는
색이 반대라, 그리는 쪽에서 매번 고르면 한 군데씩 빠뜨리게 된다.

**어둡게 쓸지 밝게 쓸지는 바탕색의 밝기가 정한다.** BT.601 가중 밝기
(`r*299 + g*587 + b*114`)가 140 이상이면 어두운 글자를 쓴다 — 사람 눈은 초록에
민감하고 파랑에 둔해서 단순 평균으로는 갈리지 않는다. 관리자가 어떤 색을 넣어도
글자가 읽히게 하려는 것이다.

흰 바탕에서는 방향색도 한 단계 진한 쪽을 쓴다(`C_EARN_D`·`C_SPEND_D`) — 밝은
초록은 흰 종이 위에서 눈에 잡히지 않는다.

**그림은 흰색에 합성해 둔다.** RGB565 에는 투명도가 없어서 서버(`talentArt.js`)와
`tools/make-artwork.py` 가 알파를 미리 배경색에 합성한다. 검정에 합성한 그림을 흰
바탕에 올리면 검은 네모가 남는다. → 이미 올려 둔 포인트·메뉴 그림은 **한 번 다시
올려야** 흰 바탕에 맞는다(배경 사진은 그대로 두면 된다 — 어두운 막이 씌워져 있고
그 위는 계속 흰 글자다).

### 항목별 포인트 (메뉴) — 더는 쓰지 않는다

예전에는 화면 오른쪽 108px 에 항목 목록(`earnMenu`/`spendMenu`)을 띄우고 눌러서 골랐다.
"무엇을 얼마나" 를 지급/사용 카드가 말하게 되면서 그 목록은 눌러도 처리에 아무 영향이
없어졌고, 화면에서 걷어냈다. 서버 설정에는 그대로 남아 있지만 **리더는 읽지 않는다.**

### 이름 표시

부팅할 때 `GET /api/talent/roster` 로 UID→이름 목록을 받아 둔다. 태그를 대는 순간
이름을 띄우려면 서버 왕복(1초 가까이)을 기다릴 수 없기 때문이다. 이름이 없는
키링은 UID 를 그대로 보여준다.

### 아이 사진

관리 화면이나 아이 조회 화면(`jesusdream.kr/t/<토큰>`)에서 올린 사진을 **카드 대기 화면의
이름 왼쪽에 48px 동그라미**로 띄운다. 이름과 같은 이유로 태그를 댈 때 받지 않고 **미리** 받아 둔다.

| 무엇 | 어떻게 |
|---|---|
| 목록 | `/roster` 의 한 줄에 `img: "ph-<무작위24자>-48.565"` 가 붙는다(사진이 없으면 칸이 없다) |
| 받기 | `GET /api/talent/photo/device/<img>` (기기 키) — 48×48 RGB565, 동그랗게 잘라 바깥은 비침색 |
| 두는 곳 | LittleFS `/art` — 그림과 같은 자리. 한 장 4.6KB 라 200명이어도 1MB 안쪽이다 |
| 언제 | 대기 화면에서 5초 동안 입력이 없을 때 **한 장씩**(`photoSyncStep`). 한꺼번에 받으면 그동안 태깅이 굳는다 |
| 바뀌면 | 사진을 바꾸면 이름(무작위 값)이 달라져 새로 받는다. 이름표는 설정 주기(`configTtlSec`, 기본 5분)마다 다시 받는다 |
| 지우기 | 이름표에 없는 `ph-` 파일은 `artPrune` 이 지운다. 단 **이번에 이름표를 못 받았으면 지우지 않는다**(`rosterFresh`) — 부팅 때 서버가 꺼져 있으면 사진이 모두 사라질 뻔했다 |

아직 못 받은 아이는 사진 없이 예전 모양(이름 왼쪽 끝, "카드를 대주세요" 가운데)으로 나온다.
사진은 구분선 위에서 끝나서 **카드 목록 자리는 줄지 않는다.** 크기를 바꿀 때는 `.ino` 의 `PHOTO_PX` 와
서버 `talentPhoto.DEVICE_PX` 를 함께 바꿀 것 — 파일 이름에 크기가 들어가서 새 크기로 다시 받는다.

### 내역 보기

`GET /api/talent/feed?limit=<화면에 들어가는 줄 수>&page=<쪽>` 을 열 때마다 받는다
(30초 안에 다시 열면 그대로 쓴다). 서버는 **기록된 시각(`at`) 기준 최신순**으로 주고,
`page` 0 이 가장 최근 묶음이다. 열 때는 늘 0쪽부터 — 지난번에 넘겨 둔 쪽에서
시작하면 방금 찍힌 것이 안 보여 "안 들어갔다"로 읽힌다.

내용 영역 위·아래에 페이지 띠를 하나씩 둔다. 위는 더 최근(이전 페이지), 아래는 더
예전(다음 페이지)이다. `limit` 을 화면에 들어가는 줄 수와 같게 맞춰 두어, 넘길 때
몇 건이 소리 없이 건너뛰어지는 일이 없다. 다음 쪽이 남았는지는 서버가 `hasMore` 로
알려 준다 — 끝에 닿으면 띠가 회색이 되고 눌러도 아무 일도 하지 않는다.

"3분 전" 은 서버가 준 `agoSec` 에 기기가 깨어 있던 시간을 더해 센다 — 리더에는
RTC 도 NTP 도 없다.

**내역 보기에서 키링을 대면** 지급도 사용도 하지 않고 그 키링의 잔액과 최근 4건을
보여준다(`GET /api/talent/feed?uid=...`, 5초). 잔액과 내역을 한 번에 받는 이유는
두 번 왕복하면 대고 나서 화면이 뜰 때까지가 눈에 띄게 늘어지기 때문이다.
등록되지 않은 키링이면 잔액 0 을 크게 띄우는 대신 "아직 등록되지 않은 키링입니다"
라고 말한다 — 0포인트인 사람으로 읽히지 않게.

## 와이파이

깨어날 때 붙는다(각 8초 대기). 인증정보는 세 군데서 오고, 이 순서로 시도한다:

1. **NVS(`talentwifi`)에 저장된 것** — 블루투스로 받아 둔 것
2. **`ChurchSecrets.h` 의 1순위(`WIFI_SSID_0`)** — 리더를 두는 교회 공유기. 있을 때만. NVS 를 지워도 늘 돌아갈 자리다
3. **`ChurchSecrets.h` 의 2순위(`WIFI_SSID`)** — 다른 스케치도 함께 쓰는 공용 값
4. **`ChurchSecrets.h` 의 3순위(`WIFI_SSID_2`)** — 있을 때만
5. 모두 안 되면 → **블루투스 설정 모드**

> ESP32-S3 는 **2.4GHz 와이파이만** 붙는다. 5GHz 전용 SSID(이름에 `_5G` 가 붙는 경우가 많다)는 목록에 있어도 붙지 못하고
> 8초를 기다린 뒤 다음 후보로 넘어간다 — 같은 공유기의 2.4GHz SSID 를 넣어야 한다.

같은 SSID 는 한 번만 시도한다(저장된 것과 1순위가 같으면 건너뛴다).

끊긴 채로도 화면은 뜨고, 태깅하면 "처리하지 못했습니다 / 네트워크 없음" 으로 알린다.
돌아가는 중에 끊기면 30초마다 조용히 다시 붙어 보고 — 위 후보를 하나씩 번갈아 건다 — (공유기 재부팅 정도는 사람이
손대기 전에 스스로 복구한다), 다시 붙는 순간 설정·이름표·카드·그림을 한 번 받는다.

### 블루투스로 와이파이 설정하기

공유기를 바꾸거나 비밀번호가 달라지면 기기가 먹통이 된다. 예전에는 그때마다
`ChurchSecrets.h` 를 고쳐 다시 구워야 했다 — 현장에 노트북과 케이블을 들고 가야 한다.
이제는 못 붙으면 스스로 블루투스를 열고 기다린다.

**들어가는 길**

* 부팅할 때 못 붙으면 자동으로 (5분 뒤에는 접고 끊긴 채로 계속한다)
* 돌아가는 중에는 헤더 띠의 **'끊김'** 을 눌러서

**화면**: 광고 이름(`funfun-<TALENT_DEVICE_ID>`)과 상태 한 줄, 아래에 **다시 시도**(알고
있는 것으로 한 번 더) · **건너뛰기**(끊긴 채로 쓴다) 두 단추.

**GATT** — 서비스 `7f3e1a00-4b21-4c8e-9a11-2d5f6c8e2001`

| 캐릭터리스틱 | 속성 | 내용 |
|---|---|---|
| `…2002` WIFI | WRITE | `{"ssid":"우리교회","pass":"비밀번호"}` 넣고 곧바로 붙어 본다<br>`{"cmd":"scan"}` 주변 목록을 훑는다 · `{"cmd":"forget"}` 저장해 둔 것을 지운다 |
| `…2003` STATE | READ·NOTIFY | `{"state":"wait\|scan\|try\|ok\|fail","ssid":"…","ip":"…","msg":"…"}` |
| `…2004` SCAN | READ | `[{"ssid":"…","rssi":-52,"lock":true}, …]` 최대 10개 |

붙으면 NVS 에 저장하고 다음 부팅부터 그것을 먼저 쓴다.

**안전**: 비밀번호는 절대 읽어 주지 않는다(WIFI 는 쓰기 전용, STATE 에는 이름만).
광고는 설정 모드일 때만 하고, 나올 때 `NimBLEDevice::deinit(true)` 로 메모리까지
돌려준다 — 평소 동작에는 블루투스가 아예 없는 것과 같다. 짝짓기(pairing)는 걸지
않았다. 화면 앞에 서서 5분 안에 넣는 작업이고, 잘못 들어와도 할 수 있는 일은
"이 기기를 다른 공유기에 붙이는 것" 뿐이다.

휴대폰 쪽은 nRF Connect 같은 범용 앱으로 바로 된다(WIFI 에 위 JSON 을 그대로 쓴다).

## 배선

전원은 TFT·PN532·부저 모두 3.3V, 공통 GND.

**핀 번호는 두 곳에서 온다.** 화면·터치의 SPI 핀은 `build.sh` 의 빌드 플래그가 정하고
(TFT_eSPI 가 그 값을 받는다), 나머지는 `.ino` 의 `PIN_*` 이 정한다. 고칠 때는 그 두
곳만 보면 된다 — 이 표는 사본이다.

| 기능 | GPIO | 정하는 곳 |
|------|------|-----------|
| TFT CS / DC / RST | **8 / 17 / 18** | `build.sh` |
| TFT MOSI / SCK / MISO | **16 / 15 / 6** | `build.sh` |
| TFT 백라이트 (PWM) | **7** | `.ino` `PIN_TFT_BL` |
| **터치 T_CS (XPT2046)** | **5** | `build.sh` `-DTOUCH_CS` |
| **터치 T_IRQ** | **4** (딥슬립 기상용, `sleepEnabled` 일 때만 필요) | `.ino` `PIN_TOUCH_IRQ` |
| PN532 SDA / SCL | **9 / 10** | `.ino` `PIN_NFC_SDA/SCL` |
| 부저 | **11** | `.ino` `PIN_BUZZER` |

### 왜 이런 번호인가

번호만 보면 뒤죽박죽이지만, **보드 왼쪽 헤더에 꽂는 자리는 TFT 14핀 헤더의 신호
순서와 그대로 맞는다.** 손배선에서 중요한 것은 번호가 아니라 구멍의 순서다 —
옆으로 나란히 가면 선이 안 꼬인다.

```
보드 왼쪽 헤더(위→아래)        TFT 14핀 헤더(위→아래)
 4번째  GPIO4  ────────────   1  T_IRQ
 5번째  GPIO5  ────────────   4  T_CS
                              2  T_DO  ┐ 모듈 쪽에서 짧게 물린다
                              3  T_DIN ┤ (기판 안에서 이어져 있지 않다)
                              5  T_CLK ┘
 6번째  GPIO6  ────────────   6  SDO(MISO)
 7번째  GPIO7  ────────────   7  LED
 8번째  GPIO15 ────────────   8  SCK
 9번째  GPIO16 ────────────   9  SDI(MOSI)
10번째  GPIO17 ────────────  10  DC
11번째  GPIO18 ────────────  11  RESET
12번째  GPIO8  ────────────  12  CS
                             13  GND → 헤더 맨 아래 GND
                             14  VCC → 헤더 맨 위 3V3
```

13·14번째 구멍(GPIO3·GPIO46)은 스트래핑 핀이라 건너뛰고, 그 아래 15~17번째를
PN532(9·10)와 부저(11)에 준다. 피한 핀: 0·3·45·46(스트래핑), 19·20(USB),
43·44(UART0).

터치 패널은 화면과 **같은 SPI 버스**를 쓰고 CS 만 따로 받는다. TFT_eSPI 가 XPT2046 을
직접 다루므로 별도 라이브러리가 필요 없고, `build.sh` 의 `-DTOUCH_CS=5` 하나로 켜진다.

`T_IRQ` 는 평소에는 쓰지 않는다(폴링). 딥슬립을 켠 기기만 화면 터치로 깨어나야 해서
RTC 핀인 GPIO4 에 물리고 `esp_sleep_enable_ext0_wakeup()` 으로 받는다.
**GPIO4 정전식 패드는 더 쓰지 않는다** — 터치 임계값 실측(`CALIBRATE_TOUCH`)도 함께 없앴다.

터치 좌표 보정값은 `.ino` 의 `TOUCH_CAL` 에 있다. 패널마다 달라 새 모듈로 바꾸면
TFT_eSPI 의 `Touch_calibrate` 예제로 다시 뽑아야 한다 — 그대로 두면 단추가 눌리는
자리가 어긋난다.

PN532 의 **IRQ·RESET 은 연결하지 않는다.** 스케치에서 `Adafruit_PN532(-1, -1, &Wire)`
로 만들어 I2C RDY 바이트로 준비 여부를 확인한다. 모듈의 모드 스위치는 **I2C** 로 둘 것.

## 빌드

```bash
./build.sh              # 컴파일
./build.sh --upload     # 컴파일 + 업로드
./build.sh --upload /dev/cu.usbmodem2101   # 포트 지정
```

TFT_eSPI 는 설정을 라이브러리 폴더(`User_Setup_Select.h`)에서 읽는다. 이 PC 에는 TTGO
T-Display 용 Setup25 가 활성화돼 있어 그대로 쓰면 서로 덮어쓴다. 그래서 라이브러리를
건드리지 않고 `-DUSER_SETUP_LOADED` 와 핀 정의를 **이 스케치에만** 주입한다.

그 플래그 목록은 스케치 폴더의 **`build_opt.h`** 한 곳에 있다. 아두이노 빌더가 이 파일을
컴파일러에 그대로 넘겨 주므로(`platform.txt` 의 `build.opt.name`), `build.sh` 로 굽든
Arduino IDE 로 굽든 같은 설정이 먹는다. **핀을 바꾸면 이 파일만 고치면 된다.**

필요 라이브러리 (NimBLE 는 블루투스 와이파이 설정에 쓴다):

```bash
arduino-cli lib install "TFT_eSPI" "Adafruit PN532" "NimBLE-Arduino"
```

### Arduino IDE 로 구울 때

IDE 에는 스케치마다 컴파일 플래그를 주는 칸이 없지만, 위 `build_opt.h` 덕분에 보드
설정만 맞추면 된다. **도구** 메뉴에서:

| 항목 | 값 |
|---|---|
| 보드 | **ESP32S3 Dev Module** (esp32 코어 3.x) |
| PSRAM | **OPI PSRAM** |
| Flash Size | **16MB (128Mb)** |
| Partition Scheme | **Custom** — 스케치 폴더의 `partitions.csv` 를 쓴다 |
| USB CDC On Boot | **Enabled** — 네이티브 USB 포트로 로그를 본다. UART 브리지 포트로 보려면 Disabled |
| 나머지 | 기본값 그대로 |

FQBN 으로는 `esp32:esp32:esp32s3:PSRAM=opi,FlashSize=16M,CDCOnBoot=cdc,PartitionScheme=custom`
— `build.sh` 가 쓰는 것과 같은 값이다.

한글 폰트 헤더(`FontKR14.h`·`FontKR20.h`)는 저장소에 없다(생성물). 클론 직후라면
`tools/make-fonts.sh` 를 한 번 돌려야 컴파일된다.

굽기 전에 **시리얼 모니터를 닫을 것** — 열려 있으면 포트를 잡고 있어
`Resource busy` 로 업로드가 실패한다.

## 문서 배포

관리자 화면(youthvision.co.kr/admin/talent)에서 볼 수 있는 것들이다.

| 무엇 | 어디 | 원본 |
|------|------|------|
| 회로도 | '기기 문서 → 회로도' · 정적 PDF `/talent/schematic.pdf` | `TalentNfcReader_회로도.pdf` |
| 매뉴얼 | '기기 문서 → 매뉴얼' · Vue 화면 `/talent/manual` | `yvServer/.../TalentManualView.vue` |
| 기기 화면 | **'리더 설정' 다이얼로그 안** | `yvServer/.../components/TalentScreen.vue` |

여기 있던 `manual.html` 은 지웠다 — 내용이 `TalentManualView.vue` 로 옮겨갔고, 두 벌을
두면 갈라진다(실제로 메뉴 기능을 넣을 때 한쪽만 고쳐질 뻔했다). 옛 내용은 git 이력에 있다.

기기 화면을 별도 문서가 아니라 설정 안에 둔 이유: 설정을 고치는 자리와 그 결과를
보는 자리가 떨어져 있으면 "밝기 10 이 얼마나 어두운지", "제목이 몇 글자까지 들어가는지"
를 확인하러 둘을 오가야 한다. 지금은 값을 고치면 옆에서 바로 다시 그려진다.

매뉴얼을 정적 HTML 이 아니라 Vue 로 둔 이유: iframe 안의 문서는 바깥 창 폭을 모르기
때문에 휴대전화에서 가로로 넘쳤다.

여기 있는 `../TalentNfcReader.html` 은 **서버와 무관한 단독 시뮬레이터**로 남겨 둔다.
태깅·오류·딥슬립까지 돌려 볼 수 있고 브라우저에서 파일을 바로 열 수 있어, 펌웨어를
고치며 화면을 맞출 때 쓴다. 서버 쪽 `TalentScreen.vue` 는 설정 미리보기용이라 더 단순하다.
**화면을 바꾸면 `.ino` · 이 파일 · `TalentScreen.vue` 셋을 함께 고칠 것.**

```bash
./make_schematic.sh                                  # 회로도 PDF 생성
cd ../../yvServer && ./scripts/sync-talent-docs.sh   # PDF·로고를 public/talent/ 로 복사
```

## 교회 로고

헤더 왼쪽의 로고는 `ChurchLogo.h` 다 — `ChurchLogoDisplay/app_icon2.png` 에서 심볼
(십자가·종·비둘기)만 잘라 **35×30 RGB565** 로 줄인 것(2,100바이트). 워드마크
("주님이 꿈꾸신 교회")는 240px 폭에서 읽히지 않아 뺐다.

흰 여백을 검정으로 바꾸지 않고 그대로 둔 이유: 십자가가 짙은 남색이라 검은 배경에서는
사라진다. 그래서 `drawHeader()` 가 헤더 42px 를 **흰 띠**로 깔고 그 위에 그린다.

## 한글 표시

TFT_eSPI 내장 폰트는 ASCII 전용이라 한글이 깨진다. `FontKR14.h` 를 스무스폰트(VLW)로
구워 넣었다 — **14px, ASCII 95자 + KS X 1001 상용 2350자** (2,445 글리프 / 473KB).

**VLW 는 구워 넣은 크기로만 그려진다.** `setTextSize()` 가 먹지 않아 크기마다 한 벌씩
든다 — `FontKR14.h`(473KB)와 `FontKR20.h`(916KB) 둘이다.

| 크기 | 쓰는 곳 |
|------|---------|
| 20px | 화면 제목 · 탭 · 이름 · "키링을 대주세요" · 오류 첫 줄 — 멀리서 읽혀야 하는 줄 |
| 14px | 내역 목록 · 부가 문구 — 전부 키우면 내역 여덟 줄이 안 들어간다 |
| 내장 6번(48px) · 4번(26px) | 잔액·포인트 숫자 (숫자뿐이라 내장 폰트로 충분) |

전환은 `useFont(px)` 하나로 한다. `loadFont()` 가 글리프 메트릭 2,445개를 힙에
올리므로(약 20KB), **같은 폰트를 다시 올리지 않게 막아 두었다** — 없으면 화면 한 장
그리는 동안 열 번 넘게 재할당된다. 숫자를 내장 폰트로 그릴 때는 `useFont(0)` 로 내려야 한다.

두 벌을 들고도 3MB 파티션의 **81%**, RAM 17% 다.

예전에는 화면에 쓰는 낱말 129자만 담았는데, 서버(`deviceLabel`)가 문구를 정하게 되면서
그 방식으로는 부족해졌다. 지금은 관리자가 무엇을 입력하든 대체로 그려진다.

```bash
./../tools/make-fonts.sh          # FontKR14.h · FontKR20.h 재생성 (커밋하지 않는 생성물)
```

한글 음절 전체(11,172자)는 14px 에서 2.4MB 라 Huge APP(3MB)에 앱과 함께 들어가지 않아
상용 2350자로 맞췄다. 그 밖의 글자는 서버가 저장 단계에서 막는다.

증감 숫자는 크게 보여야 해서 내장 폰트(6번)를 쓰는데, 스무스폰트가 올라가 있으면
폰트 번호가 무시되므로 그 구간만 `krFont(false)` 로 잠깐 내렸다가 올린다.

## 터치 임계값 보정 (처음 한 번 필요)

보드·패드마다 값이 다르다. 스케치 상단의 `CALIBRATE_TOUCH` 를 1 로 바꿔 굽고,
시리얼에 찍히는 값을 손 뗐을 때와 댔을 때 각각 확인한 뒤 중간값을 `TOUCH_THRESH` 에 넣는다.

```cpp
#define CALIBRATE_TOUCH 1
```

ESP32-S3 는 **터치하면 값이 커진다**(구형 ESP32 와 반대). 깨우기도 "임계값 이상"으로 건다.

## 소리

부저는 GPIO 와 GND 사이에 그냥 물려 있다 — 트랜지스터도 증폭단도 없다. 그래서
소리 크기를 키울 손잡이는 **주파수** 하나뿐이다. 얇은 피에조 판은 자기 공진점
언저리(12~14mm 판이면 대략 2.3~3.0kHz)에서만 제대로 울고, 거기서 멀어지면 같은
전압을 줘도 소리가 급격히 작아진다. 예전 값(300~2000Hz)은 대부분 그 아래여서
"울리지 않고 딸깍대는" 소리가 났다 — 특히 실패음 300Hz 는 행사장에서 사실상
들리지 않았다. 그래서 전부 2.0~3.2kHz 안으로 옮겼다.

뜻을 나누는 것은 세기가 아니라 **방향과 개수**라, 대역을 옮겨도 "오르면 지급,
내리면 사용" 은 그대로다.

| 이벤트 | 패턴 |
|--------|------|
| 전원 ON | 2093 → 2637 → 3136Hz (상승 3연음) |
| 전원 OFF | 3136 → 2637 → 2093Hz (하강 3연음) |
| 지급 | 2637 → 3136Hz (짧게 위로) |
| 사용 | 3136 → 2093Hz (짧게 아래로) |
| 실패 | 1976Hz 두 번 (대역 안에서 가장 낮은 쪽) |
| 화면 전환 (내역 · 취소) | 2637Hz 짧게 |

음 사이에 18ms 무음을 둔다(`BUZ_GAP`). 붙여서 내면 두 음이 한 음처럼 뭉개져
지급(2연음)과 화면 전환(1음)이 구별되지 않는다. `noTone()` 뒤에 핀을 LOW 로 내리는
것도 같은 이유다 — HIGH 로 남으면 판이 눌린 채 있어 다음 음이 둔해진다.

`beep()` 이 재생 시간만큼 붙잡아 두므로 딥슬립 직전 전원 OFF 음도 잘리지 않는다.

## 상태 LED

보드에 달린 RGB LED(WS2812)로 지금 상태를 알린다. 화면을 보지 않아도 — 줄 뒤쪽이나 옆에서도 — 보인다.

| 색 | 언제 |
|---|---|
| 빨강 | 오류. 실패음이 나는 모든 자리(`sndFail`)에서 켜고 3초 둔다. 무음 기기에서도 켠다 |
| 초록 | 키링을 알아봤다 — 카드 대기·완료 화면 동안, 내역에서 잔액을 조회하면 5초 |
| 노랑 | 연결 중 — 와이파이에 안 붙어 있다(부팅 때 붙는 중, 블루투스 설정 대기, 끊겨서 30초마다 다시 붙는 중). 0.3초로 빠르게 깜빡인다. 이때는 태깅해도 서버에 못 묻는다 |
| 파랑 | 평소 대기 — 1초 켜짐 · 1초 꺼짐으로 천천히 깜빡인다 |

색이 겹치면 위에서부터 이긴다: 오류 빨강·조회 초록(잠시 붙잡은 색) → 키링 초록 → 연결 중 노랑 → 대기 파랑.
부팅 때 붙기를 기다리는 동안(`wifiTry`)과 블루투스 설정 화면에서는 loop 가 돌지 않아 그 자리에서 LED 를 갱신한다.

밝기는 `LED_LEVEL`(24/255)이다. 보드 LED 는 바로 보면 눈부시다. 색이 바뀔 때만 LED 에 쓴다(한 번에 1ms 남짓).
부팅할 때와 딥슬립 직전에 끈다 — WS2812 는 칩이 리셋돼도 전원이 붙어 있으면 이전 색을 들고 있다.

**핀.** 코어(esp32 3.3.11)의 `esp32s3` 변형이 `RGB_BUILTIN` 을 **GPIO48** 로 둔다. ESP32-S3-DevKitC-1 **v1.0** 의
자리다. **v1.1** 보드는 LED 가 **GPIO38** 로 옮겨졌으니 `build_opt.h` 에 한 줄을 넣는다:

```
-DSTATUS_LED_PIN=38
```

48·38 모두 이 스케치의 다른 핀, N16R8 의 PSRAM(GPIO35~37), USB(19·20)와 겹치지 않는다.
LED 가 안 켜지면 보드 판의 표기(v1.0 / v1.1)를 보고 위 한 줄을 넣거나 뺀다.

## 달란트 저장 — 서버 DB

잔액은 **yvServer(youthvision.co.kr)의 MongoDB** 가 단일 출처다.
같은 API 가 jdServer(jesusdream.kr)에도 올라가 있어, 접속 서버를 바꾸려면
`ChurchSecrets.h` 의 `TALENT_API_BASE` 한 줄만 고치면 된다(기기 키는 두 서버가 같다). 리더는 저장하지 않고
매번 서버에 묻는다. 리더를 여러 대 놓아도 잔액이 하나로 모이고, 지급·사용 내역이
서버에 남아 "왜 이 잔액인지" 설명할 수 있다.

| 메서드 | 경로 | 용도 |
|--------|------|------|
| POST | `/api/talent/earn` | 지급 (기기 키 필요) — `card` UID 를 보내면 금액은 서버가 찾는다 |
| POST | `/api/talent/spend` | 사용 (잔액 부족 시 409) |
| GET | `/api/talent/cards` | 지급/사용 카드 목록 (기기 키 또는 관리자) |
| PUT/DELETE | `/api/talent/cards/:uid` | 카드 등록·수정·삭제 (관리자) |
| POST | `/api/talent/seen` | 처음 보는 UID 알림 (기기 키) |
| GET/DELETE | `/api/talent/seen` | 미등록 카드 목록·치우기 (관리자) |
| GET | `/api/talent/:uid` | 잔액 조회 (공개) |
| GET | `/api/talent` | 전체 목록 (관리자) |
| GET | `/api/talent/logs?uid=&limit=` | 증감 내역 (관리자) |

요청/응답:

```
POST /api/talent/earn
  헤더 : x-talent-key: <TALENT_DEVICE_KEY>
  본문 : { "uid": "04AABBCCDD", "card": "04C1D2E3", "device": "nfc-reader-1" }
  응답 : { "success": true, "uid": "...", "balance": 7, "delta": 5, "item": "출석" }

  card 대신 amount 를 보내는 예전 방식도 계속 받는다(메뉴를 쓰는 기기).
  card 를 주면 금액·이름·분류는 서버가 그 카드에서 찾는다.
```

**기기 키**: 달란트는 포인트라 인증 없이 열면 누구나 지급할 수 있다. 그래서 쓰기는
`x-talent-key` 헤더를 서버의 `TALENT_DEVICE_KEY`(.env)와 대조한다. 기기 쪽 값은
`~/Documents/Arduino/libraries/ChurchSecrets/ChurchSecrets.h` 에 있다. 두 값이 같아야 한다.

**네트워크가 끊기면 처리하지 않는다.** 화면에 사유를 띄우고 실패음을 낸다.
잔액은 건드리지 않으므로 나중에 다시 태깅하면 된다. (오프라인 대기열은 넣지 않았다)

## 서버에서 내려받는 설정

부팅할 때 `GET /api/talent/config?device=<TALENT_DEVICE_ID>` (헤더 `x-talent-key`) 로 설정을 받아
NVS(`talentcfg`)에 캐시한다. 화면은 **캐시로 먼저 그리고** 설정 갱신은 그 뒤에 하므로,
서버가 느리거나 꺼져 있어도 깨어나는 속도는 그대로다. 서버에 못 붙으면 캐시 → 기본값 순으로 물러선다.

| 키 | 기본값 | 범위 | 하는 일 |
|---|---|---|---|
| `talentStep` | 1 | 1~1000 | 카드가 없던 시절의 값 — 지금은 쓰이지 않는다 |
| `attendanceMode` | false | | 켜면 카드 단계를 건너뛴다(키링 한 번으로 지급) |
| `attendanceCard` | '' | 20자 | 출석모드에서 쓸 카드 UID (비우면 첫 카드) |
| `doneMs` | 3000 | 1000~15000 | 완료 그림을 띄워 두는 시간 |
| `sleepTimeoutMs` | 15000 | 5000~600000 | 무입력 후 딥슬립까지 |
| `tagCooldownMs` | 1500 | 200~10000 | 같은 키링 연속 처리 방지 |
| `touchDebounceMs` | 400 | 100~3000 | 터치 연속 인식 방지 |
| `httpTimeoutMs` | 6000 | 1000~20000 | 서버 요청 상한 |
| `rotation` | 0 | 0~3 | 거치 방향 (**다음 부팅에 적용**) |
| `backlight` | 255 | 10~255 | 백라이트 밝기 |
| `sound` | true | | 부저 on/off |
| `bgColor` | `#EBAC42` | `#RRGGBB` | 내용 영역 바탕색 (배경 그림이 있으면 그림이 이긴다) |
| `deviceLabel` | 달란트 | 10자 | 화면 왼쪽 위 제목 |
| `configTtlSec` | 300 | 30~86400 | 깨어 있는 동안 재조회 주기 |

깨어 있는 동안에는 `configTtlSec` 마다 다시 받는다. 행사 중에 관리자가 값을 바꾸면
기기를 만지지 않아도 반영된다(`rotation` 만 예외 — 화면을 다시 그려야 해서 다음 부팅에 적용).

### 서버에서 못 바꾸는 값

- **`TOUCH_THRESH`** — 딥슬립 기상 조건이라 잘못된 값을 받으면 터치로 못 깨어나고
  설정을 다시 받으러 갈 수도 없다. 전원을 뽑아야만 복구되므로 펌웨어에 고정한다.
- **WiFi/서버 주소/기기 키** (`ChurchSecrets.h`) — 이걸로 서버에 접속하므로 서버에서 받을 수 없다.
- **핀 정의** — 하드웨어 고정값.

### 화면 문구에 쓸 수 있는 글자

`FontKR14.h` 에는 **ASCII 95자 + KS X 1001 상용 2350자**가 14px 로 구워져 있다(총 2,445 글리프, 473KB).
실제 한국어 문서에 쓰이는 음절은 사실상 이 안에 다 들어오므로, `deviceLabel` 은 자유롭게 정해도 된다.

한글 음절 전체(11,172자)를 담으면 14px 에서 **2.4MB** 라 Huge APP(3MB)에 앱과 함께 들어가지 않는다.
그래서 상용 2350자로 맞췄다. 그 밖의 글자(예: 뷁)를 넣으면 서버가 저장 단계에서 막고 알려준다.

폰트를 다시 만들려면:

```bash
./tools/make-fonts.sh
```

글자 집합을 바꿀 때는 `yvServer/talent/talentConfig.js` 의 `FONT_HANGUL` 규칙도 함께 맞춰야 한다
(지금은 양쪽 다 "EUC-KR 로 인코딩되는 한글" 이라는 같은 규칙으로 만들어 쓴다).

## 아직 확인하지 못한 것

실물 보드가 없어 **컴파일과 폰트 검증까지만** 했다. 다음은 기기에서 봐야 한다.

- 터치 임계값 (위 보정 절차 필요)
- PN532 인식 — 못 찾으면 화면에 "NFC 모듈 없음" 이 뜬다
- 딥슬립 복귀 후 TFT 재초기화
- 화면 레이아웃 (240x320 세로 기준으로 좌표를 잡았다)
- WiFi 접속 시간 — 깨어날 때마다 붙으므로 체감 지연이 얼마나 되는지
