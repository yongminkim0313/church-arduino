# ── 생성물(파생 파일) ──────────────────────────────────────────────
# 한글 스무스폰트 헤더는 tools/ttf2vlw.py 로 1초면 다시 만들어진다.
# 11MB 짜리라 커밋하면 폰트를 손볼 때마다 이력에 11MB 씩 쌓인다.
# 클론 후 tools/make-fonts.sh 를 한 번 돌릴 것.
*/FontKRFull12.h
*/FontNum38.h
*/FontKR16.h
*/FontKR14.h
*/FontNum30.h
*.vlw

# 빌드 산출물
build/
*.bin
*.elf
*.map

# 인증정보 — 실제 값은 저장소 밖(~/Documents/Arduino/libraries/ChurchSecrets/)에 둔다.
# secrets.example.h 만 템플릿으로 남긴다.
ChurchSecrets.h
secrets.h

# 로컬 설정
**/sketch.yaml.local

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Node (테스트 서버)
node_modules/
